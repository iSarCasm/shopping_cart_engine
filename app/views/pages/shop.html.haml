%section#index
  .container
    .row
      .col-sm-8
        %h2= t('main.products')
        = render @books
        .row.page-pagination
          .col-xs-12
            = paginate @books
      .col-sm-4
        %h2= t('main.categories')
        = render @categories
