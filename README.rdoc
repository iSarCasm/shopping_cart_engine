= ShoppingCart

=form_tag(add_shopping_carts_path, method: :post) do
  .div.col-xs-2
    = number_field_tag :quantity, '1', min: 1, class: 'form-control'
  = hidden_field_tag :book, book.id
  .actions
    =submit_tag t('cart.add'), class: ['btn', 'btn-primary']



This project rocks and uses MIT-LICENSE.
